import React, { Component } from 'react';

const styles = {
  container: {
    display: 'flex',
    minWidth: '100%',
    minHeight: '364px',
    backgroundImage: 'linear-gradient(to bottom right,rgba(165,107,159,.05),rgba(165,107,159,.3))',
    position: 'relative',
    top: '-109px'
  }
};

class Subheader extends Component {
  render() {
    return (
      <div style={styles.container}>

      </div>
    );
  }
}

export default Subheader;
